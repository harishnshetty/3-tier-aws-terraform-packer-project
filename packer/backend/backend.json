{
  "variables": {
    "aws_region": "ap-south-1"
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "region": "{{user `aws_region`}}",
      "source_ami": "ami-02d26659fd82cf299",
      "instance_type": "t3.micro",
      "ssh_username": "ubuntu",
      "ami_name": "three-tier-backend-flask-ubuntu-{{timestamp}}",
      "ami_description": "Custom backend AMI with Python Flask + MySQL client on Ubuntu 24.04 LTS",
      "tags": {
        "Project": "three-tier",
        "Tier": "backend",
        "Base_AMI": "ubuntu-24.04"
      }
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "set -e",
        "echo '📦 Updating system and installing required packages...'",
        "sudo apt update -y",
        "sudo apt install -y python3 python3-pip git mysql-client",
        
        "echo '📦 Installing Flask + MySQL connector + Gunicorn...'",
        "pip3 install flask mysql-connector-python gunicorn",
        
        "echo ✅ Backend AMI (Flask) preparation complete!"
      ]
    }
  ]
}
